
services:
  db:
    image: postgres:15-alpine
    container_name: kanban_postgres
    restart: always
    env_file:
      - backend/.env
    ports:
      - "5432:5432"
    volumes:
      - kanban_db_data:/var/lib/postgresql/data

volumes:
  kanban_db_data: